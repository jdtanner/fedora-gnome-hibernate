# Fedora 43 Hibernate Master Setup

One script to enable hibernation and install the GNOME UI extension.

## Usage
1. Edit `setup-fedora-hibernate.sh` with your Swap UUID.
2. Run `sudo ./setup-fedora-hibernate.sh`
3. Reboot.

**Note:** 'Automatic Suspend' in GNOME Settings is now rewired to Hibernate.
